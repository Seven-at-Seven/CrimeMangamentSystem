# Normalize all text files to use LF in the repository
* text=auto eol=lf

# Ensure Windows-specific files retain CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts should always use LF
*.sh text eol=lf
# Treat Java source files as text and normalize to LF
*.java text eol=lf

# Treat FXML files as text and normalize to LF
*.fxml text eol=lf

# Treat CSS files as text and normalize to LF
*.css text eol=lf

# Treat properties files as text and normalize to LF
*.properties text eol=lf

# Normalize plain text files
*.txt text eol=lf
*.md text eol=lf
# Ensure binary files are not modified
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.jar binary
*.class binary